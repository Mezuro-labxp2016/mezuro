# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure(2) do |config|
  config.vm.define "mezuro" do |mezuro|
    mezuro.vm.box = "debian/wheezy64"
    mezuro.vm.network "private_network", ip: "10.18.0.115"
  end

end
